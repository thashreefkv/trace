# Public OAuth client ID for a Google "Desktop app" OAuth client.
# Desktop OAuth clients must use PKCE and must not embed a client secret.
TRACE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional port for the authenticated Siri / Shortcuts API.
# TRACE_SIRI_PORT=8421
